{
	let sum = 0;
    for (x of 0..10) {
    	sum = sum + x;
    }
}

{
	for (item, index of [1, 2, 3]) {
    	console.log(item, index);
    }
}

{
    const basket = ['apple', 'orange', 'pear'];
	for (fruit of basket) {
    	console.log(fruit);
    }
}

{
    const nestedBasket = {
    	veggies: [],
        fruits: ['apple', 'orange', 'pear'],
    };
    
	for (fruit of nestedBasket.fruits) {
    	console.log(fruit);
    }
}